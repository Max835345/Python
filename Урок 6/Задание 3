num = int(input())
total = int(input())

while total != 0:
    total -= 1
    dig = num % 10
    dig2 = num // 10
    sum = dig + dig2

    if num // sum % 2 == 0:
        print("YES")
    else:
        print("NO")
